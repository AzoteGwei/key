// Deliberately malformed: the problem block never closes and the formula is nonsense.
//
// Used to check that a task which fails inside the worker is reported as failed, with the source
// positions KeY found, rather than quietly reaching a terminal state that reads like success.
\problem {
    \forall int x; (x <=
